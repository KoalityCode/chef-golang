languages:
  ruby: 1.9.3
setup:
- packages:
  - system:
    - libxslt-dev
    - libxml2-dev
- scripts:
  - bundle install
  - bundle install --gemfile test/support/Gemfile
test:
  scripts:
  - bundle exec rake foodcritic
